const { createEngineRouter } = require('../../utils/engineHandler');

const router = createEngineRouter({
  slug: 'publishing-spider',
  name: 'EC Engine 7: Publishing Spider Web',
  description: 'Multi-channel content distribution and publishing'
});

module.exports = router;
