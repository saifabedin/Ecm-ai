const { createEngineRouter } = require('../../utils/engineHandler');

const router = createEngineRouter({
  slug: 'strategy-planning',
  name: 'EC Engine 3: Strategy & Content Planning',
  description: 'AI-driven campaign strategy and content calendar planning'
});

module.exports = router;
